define([
       "jquery", "underscore", "backbone"
       , "views/snippet",
  , "bootstrap"
  , "bootstrap-multiselect"

], function (
  $, _, Backbone
  , SnippetView
) {
    return SnippetView.extend({
    });
});
